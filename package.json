{
	"name": "@prc/print-engine",
	"version": "1.0.0",
	"description": "Print-styled post preview, server-side PDF export, and a block callback registry",
	"private": true,
	"author": "Pew Research Center",
	"license": "GPL-2.0-or-later",
	"scripts": {
		"build": "wp-scripts build --webpack-copy-php --source-path=src --output-path=build",
		"start": "wp-scripts start --webpack-copy-php --source-path=src --output-path=build"
	},
	"dependencies": {
		"pagedjs": "^0.4.3"
	},
	"devDependencies": {
		"@wordpress/scripts": "^31.4.0"
	}
}
